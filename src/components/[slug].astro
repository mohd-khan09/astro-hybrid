<!-- ---
import { fetchAllTalks, fetchTalkById } from "../../services/talks";
import TalksLayout from "../../components/TalksLayout";



export async function getStaticPaths() {
  const talks = await fetchAllTalks();
  
  return talks?.map((page: any) => {
    const { videoId, slug } = page;
    
    return {
      params: { slug },
      props: { 
        page,
        videoId
      }
    };
  });
}

const { page, videoId } = Astro.props;
const { slug } = page || {};
---

<div class="event_mainWrapper">
  <TalksLayout  talk={page}   videoId={videoId}/>
</div> -->
